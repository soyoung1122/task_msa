apiVersion: apps/v1
kind: Deployment
metadata:
  name: task_msa_ui
  labels:
    app: task_msa
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task_msa
      tier: backend
  template:
    metadata:
      labels:
        app: task_msa
        tier: backend
    spec:
      containers:
        - name: task_msa
          image: soyoung1122/task_msa_ui:latest
          imagePullPolicy: Always
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: kpaas-msa-configmap
      imagePullSecrets:
        - name: dockerhub